# Hello World React

## To deploy the build to NGNIX web server

```sh
docker build -f dockerfile-nginx -t my-react-app:nginx .
```

## To run the app from nodejs

```sh
docker build -f dockerfile-react -t my-react-app:react .
```
